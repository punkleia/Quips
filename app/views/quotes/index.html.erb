<h2>
  <span>Moxie No. <%= @quote.id %></span>
  Empathy and encouragement by the (mostly) greats
</h2>

<h1><%= @quote.saying %></h1>
<div class="pull-right">
  <h2><%= @quote.author %></h2>
  <h3><%= @quote.source %></h3>
</div>

<br class="clear" />
<br />
<br />
<br />

<div class="container">
  <% if flash[:error].present? %>
    <div class="col-xs-10 col xs-offset-1 alert alert-danger">
      <%= flash[:error].html_safe %>
    </div>
  <% end %>
  <%= yield %>  
</div>
